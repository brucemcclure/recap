let pokemon = 'charmander'

const encounter = pokemon => {
  console.log(`Red encountered a wild ${pokemon}.`)
}

const pokedex = pokemon => {
  // Make a axious call to the pokemon api to make this function work
  console.log(
    `The ${name} is number ${order} in the PokeDex it weighs ${weight}`
  )
}

encounter(pokemon)
